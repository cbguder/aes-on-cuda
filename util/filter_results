#!/usr/bin/env python

import sys

encryption = []
total      = []

prefixes = [ 'Encryption alone takes ', '4194304 blocks encrypted in ' ]

for line in sys.stdin:
	for i, p in enumerate(prefixes):
		if line.startswith(p):
			parts = line.split('/')
			time = parts[0][len(p):]
			if i == 0:
				encryption.append(time)
			else:
				total.append(time)

print '\n'.join(total)
print '\n'.join(encryption)
